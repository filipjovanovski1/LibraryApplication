@* @model CoursesApplication.Domain.DomainModels.BookBorrowing
@using Microsoft.AspNetCore.Mvc.Rendering

<h2>Edit Borrowing</h2>

<form asp-action="Edit" method="post">
   
    <input asp-for="Id" type="hidden" />
    <input asp-for="BookCopyId" type="hidden" />
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <div class="mb-3">
        <label class="form-label">Book Copy</label>
        <input class="form-control" value="@ViewBag.InventoryCode ?? Model.BookCopyId.ToString()" readonly />
        
    </div>

    <div class="mb-3">
        <label asp-for="UserId" class="form-label">User</label>
        <select asp-for="UserId"
                class="form-select"
                asp-items="(IEnumerable<SelectListItem>)ViewBag.Users">
            <option value="">-- Select user --</option>
        </select>
        <span asp-validation-for="UserId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="BorrowedAt" class="form-label"></label>
        <input asp-for="BorrowedAt" class="form-control" type="date" />
        <span asp-validation-for="BorrowedAt" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="DueDate" class="form-label"></label>
        <input asp-for="DueDate" class="form-control" type="date" />
        <span asp-validation-for="DueDate" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-controller="Books" asp-action="Details" asp-route-id="@ViewBag.BookId" class="btn btn-secondary">Cancel</a>
</form> 

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@

@model CoursesApplication.Domain.DomainModels.BookBorrowing
@using Microsoft.AspNetCore.Mvc.Rendering
@{
    ViewData["Title"] = "Edit Borrowing";
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
            <li class="breadcrumb-item"><a asp-controller="BookBorrowings" asp-action="Index">Borrowings</a></li>
            <li class="breadcrumb-item active" aria-current="page">Edit</li>
        </ol>
    </nav>

    <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-7">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">Edit Borrowing</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post" novalidate>
                        @Html.AntiForgeryToken()

                        <!-- Keep Id & BookCopyId for binding -->
                        <input asp-for="Id" type="hidden" />
                        <input asp-for="BookCopyId" type="hidden" />

                        <div class="alert alert-danger d-none" role="alert" data-valmsg-summary="true">
                            <ul></ul>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Book Copy</label>
                            <input class="form-control" value="@ViewBag.InventoryCode ?? Model.BookCopyId.ToString()" readonly />
                        </div>

                        <div class="mb-3">
                            <label asp-for="UserId" class="form-label">User <span class="text-danger">*</span></label>
                            <select asp-for="UserId" class="form-select"
                                    asp-items="(IEnumerable<SelectListItem>)ViewBag.Users">
                                <option value="">-- Select user --</option>
                            </select>
                            <span asp-validation-for="UserId" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="BorrowedAt" class="form-label">Borrowed At <span class="text-danger">*</span></label>
                            <input asp-for="BorrowedAt" class="form-control" type="date" />
                            <span asp-validation-for="BorrowedAt" class="text-danger small"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="DueDate" class="form-label">Due Date <span class="text-danger">*</span></label>
                            <input asp-for="DueDate" class="form-control" type="date" />
                            <span asp-validation-for="DueDate" class="text-danger small"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Save</button>
                            <a asp-action="Index" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-muted small">
                    Fields marked with <span class="text-danger">*</span> are required.
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
