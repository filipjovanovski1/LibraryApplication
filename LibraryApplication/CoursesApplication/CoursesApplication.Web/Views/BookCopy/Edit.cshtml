@* @model CoursesApplication.Domain.DomainModels.BookCopy

<h2>Edit Book Copy</h2>

<form asp-action="Edit" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <input type="hidden" asp-for="Id" />

    <div class="mb-3">
        <label asp-for="InventoryCode" class="form-label"></label>
        <input asp-for="InventoryCode" class="form-control" />
        <span asp-validation-for="InventoryCode" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="BookId" class="form-label"></label>
        <input asp-for="BookId" class="form-control" />
        <span asp-validation-for="BookId" class="text-danger"></span>
    </div>

    <div class="mb-3">
        <label asp-for="BookBorrowingId" class="form-label"></label>
        <input asp-for="BookBorrowingId" class="form-control" />
        <span asp-validation-for="BookBorrowingId" class="text-danger"></span>
    </div>

    <button type="submit" class="btn btn-primary">Save</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
} *@


@model CoursesApplication.Domain.DomainModels.BookCopy
@{
    ViewData["Title"] = "Edit Book Copy";
}

<div class="container py-4">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index">Home</a></li>
            <li class="breadcrumb-item"><a asp-controller="BookCopy" asp-action="Index">Book Copies</a></li>
            <li class="breadcrumb-item active" aria-current="page">Edit</li>
        </ol>
    </nav>

    <div class="row justify-content-center">
        <div class="col-12 col-md-10 col-lg-7">
            <div class="card shadow-sm">
                <div class="card-header bg-warning text-dark">
                    <h5 class="mb-0">Edit Book Copy</h5>
                </div>
                <div class="card-body">
                    <form asp-action="Edit" method="post" novalidate>
                        @Html.AntiForgeryToken()

                        <input type="hidden" asp-for="Id" />
                        <input type="hidden" asp-for="BookId" />

                        <div class="alert alert-danger d-none" role="alert" data-valmsg-summary="true">
                            <ul></ul>
                        </div>

                        <div class="mb-3">
                            <label class="form-label">Book</label>
                            <input class="form-control" value="@(Model.Book?.Title ?? Model.BookId.ToString())" readonly />
                        </div>

                        <div class="mb-3">
                            <label asp-for="InventoryCode" class="form-label">Inventory Code <span class="text-danger">*</span></label>
                            <input asp-for="InventoryCode" class="form-control" />
                            <span asp-validation-for="InventoryCode" class="text-danger small"></span>
                        </div>

                        <div class="d-flex gap-2">
                            <button type="submit" class="btn btn-primary">Save</button>
                            <a asp-controller="Books" asp-action="Details" asp-route-id="@Model.BookId" class="btn btn-outline-secondary">Cancel</a>
                        </div>
                    </form>
                </div>
                <div class="card-footer text-muted small">
                    Fields marked with <span class="text-danger">*</span> are required.
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
